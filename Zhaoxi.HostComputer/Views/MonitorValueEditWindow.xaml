<Window x:Class="Zhaoxi.HostComputer.Views.MonitorValueEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Zhaoxi.HostComputer.Views"
        mc:Ignorable="d" UseLayoutRounding="True"
        FontFamily="Microsoft YaHei" FontWeight="ExtraLight" Foreground="#333"
        WindowStyle="None" AllowsTransparency="True"  WindowStartupLocation="CenterOwner" Background="Transparent"
        Title="监控点位编辑" Height="380" Width="450">
    <Window.Resources>
        <ControlTemplate TargetType="Button" x:Key="ButtonTemplate">
            <Border Background="{TemplateBinding Background}" CornerRadius="3">
                <Border Name="back">
                    <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"
                                  Content="{TemplateBinding Content}" TextBlock.Foreground="White"/>
                </Border>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#22000000" TargetName="back"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
        
        <Style TargetType="Button" x:Key="ControlButtonStyle">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Background="Transparent" Height="30" Width="45" Name="back">
                            <Border Name="hover" Background="{TemplateBinding Background}" Visibility="Collapsed"/>
                            <TextBlock Text="{TemplateBinding Content}" FontFamily="{StaticResource iconfont}" 
                                   VerticalAlignment="Center" HorizontalAlignment="Center" Name="txt"
                                   Foreground="White"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Visibility" Value="Visible" TargetName="hover"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border Margin="5" Background="#FFF6F7FC">
        <Border.Effect>
            <DropShadowEffect BlurRadius="8" Opacity="0.2" Direction="0" Color="Gray" ShadowDepth="0"/>
        </Border.Effect>
        <Grid MouseLeftButtonDown="Grid_MouseLeftButtonDown">
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>
            
            <Border Background="#FF3269DE"/>
            <TextBlock Text="监控点位编辑" Foreground="White" VerticalAlignment="Center" Margin="15,0" FontSize="16"/>
            <Button Content="&#xe653;" Style="{StaticResource ControlButtonStyle}" Background="#11000000" Tag="#333" Height="33" VerticalAlignment="Top"
                    HorizontalAlignment="Right" Click="CloseButton_Click"/>

            <StackPanel Grid.Row="1" Margin="30,20">
                <StackPanel Orientation="Horizontal" Margin="0,10">
                    <TextBlock Text="监控标签：" Width="80" VerticalAlignment="Center"/>
                    <TextBox x:Name="tbTagName" Width="280" Height="28" VerticalContentAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,10">
                    <TextBlock Text="寄存器地址：" Width="80" VerticalAlignment="Center"/>
                    <TextBox x:Name="tbAddress" Width="280" Height="28" VerticalContentAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,10">
                    <TextBlock Text="数据类型：" Width="80" VerticalAlignment="Center"/>
                    <ComboBox x:Name="cbDataType" Width="280" Height="28" SelectedIndex="0">
                        <ComboBoxItem Content="ushort"/>
                        <ComboBoxItem Content="short"/>
                        <ComboBoxItem Content="uint"/>
                        <ComboBoxItem Content="int"/>
                        <ComboBoxItem Content="float"/>
                        <ComboBoxItem Content="double"/>
                        <ComboBoxItem Content="bool"/>
                    </ComboBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,10">
                    <TextBlock Text="单位：" Width="80" VerticalAlignment="Center"/>
                    <TextBox x:Name="tbUnit" Width="280" Height="28" VerticalContentAlignment="Center"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Button Content="保存" Template="{StaticResource ButtonTemplate}" Width="80" Height="32" Margin="10,0"
                        Background="#FF3269DE" Click="SaveButton_Click"/>
                <Button Content="取消" Template="{StaticResource ButtonTemplate}" Width="80" Height="32"
                        Background="#999" Click="CloseButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
